# 📋 Инструкция по настройке

## ✅ Вариант 1: Эмулятор (рекомендуется для начала)

**НЕ требует настройки Telegram!** Работает полностью локально на вашем компьютере.

### Что нужно сделать:
1. Установить зависимости: `pip install -r requirements.txt`
2. Запустить: `python main.py --mode single`

**Готово!** Боты будут общаться через эмулятор локально.

---

## 🔵 Вариант 2: Реальный Telegram (для продакшена)

Если хотите использовать реальный Telegram, выполните шаги ниже.

---

## 📱 Шаг 1: Создать ботов в Telegram

### 1.1. Создать основного бота

1. Откройте Telegram
2. Найдите **@BotFather** (официальный бот для создания ботов)
3. Отправьте команду: `/newbot`
4. Следуйте инструкциям:
   - **Имя бота**: `Мой Тестовый Бот` (может быть любое)
   - **Username**: `mytestbot12345` (должен заканчиваться на `bot`, должен быть уникальным)
5. BotFather пришлет **токен**, например:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890
   ```
6. **Скопируйте этот токен** - это ваш `MAIN_BOT_TOKEN`

### 1.2. Создать тестового бота

Повторите шаги выше для второго бота:
- **Имя**: `Тестовый Клиент`
- **Username**: `mytestclientbot12345`
- **Скопируйте токен** - это ваш `TEST_BOT_TOKEN`

---

## 👥 Шаг 2: Создать тестовую группу

1. В Telegram создайте новую группу (New Group)
2. Дайте имя группе, например: `Тестовая группа для ботов`
3. Добавьте в группу **оба бота**:
   - Нажмите "Add members"
   - Найдите своих ботов по username (например, `@mytestbot12345`)
   - Добавьте их обоих
4. (Опционально) Сделайте ботов администраторами группы

---

## 🔍 Шаг 3: Получить Chat ID группы

### Способ 1: Через @userinfobot (самый простой)

1. Добавьте бота **@userinfobot** в вашу группу
2. Отправьте любое сообщение в группу
3. Бот покажет информацию, найдите `chat id: -123456789` (отрицательное число)

### Способ 2: Через API

1. Отправьте любое сообщение в группу (можете написать "test")
2. Откройте в браузере (замените `<ВАШ_ТОКЕН>` на токен одного из ваших ботов):
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
   Например:
   ```
   https://api.telegram.org/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890/getUpdates
   ```
3. Найдите в ответе `"chat":{"id":-123456789}` - это ваш `TEST_GROUP_ID`

### Способ 3: Через скрипт (автоматически)

Я могу создать скрипт, который автоматически найдет chat_id для вас.

---

## 📝 Шаг 4: Создать файл .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Telegram Bot Tokens
MAIN_BOT_TOKEN=ваш_токен_основного_бота_здесь
TEST_BOT_TOKEN=ваш_токен_тестового_бота_здесь

# Test Group ID (отрицательное число)
TEST_GROUP_ID=-123456789

# Режим работы
USE_REAL_TELEGRAM=true
EMULATOR_MODE=false

# AI Agent (опционально - для автоматических исправлений)
AI_API_KEY=ваш_openai_api_ключ_здесь
AI_MODEL=gpt-4

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/conversations.log
```

**Пример:**
```env
MAIN_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890
TEST_BOT_TOKEN=9876543210:XYZabcDEFghiJKLmnopQRS-9876543210
TEST_GROUP_ID=-1001234567890
USE_REAL_TELEGRAM=true
EMULATOR_MODE=false
```

---

## 📤 Шаг 5: Что мне отправить

Отправьте мне следующую информацию:

1. ✅ **MAIN_BOT_TOKEN** - токен основного бота
2. ✅ **TEST_BOT_TOKEN** - токен тестового бота
3. ✅ **TEST_GROUP_ID** - ID группы (отрицательное число, например `-123456789`)

**ИЛИ** просто скажите "настрой для реального Telegram" и отправьте токены, я настрою всё сам.

---

## ⚠️ ВАЖНО: Безопасность

- ❌ **НЕ публикуйте** токены в публичных местах
- ❌ **НЕ коммитьте** файл `.env` в git
- ✅ Файл `.env` уже добавлен в `.gitignore`
- ✅ Токены дают полный доступ к ботам

---

## 🚀 После настройки

После того как я настрою файл `.env`, запустите:

```bash
python main.py --mode continuous --cycles 5
```

Система начнет:
1. Запускать ботов в Telegram
2. Тестировать их общение
3. Находить ошибки
4. Автоматически улучшать код

---

## 💡 Рекомендация

Для начала рекомендую использовать **эмулятор** - он работает сразу без настройки Telegram, и вы можете протестировать всю систему локально. Потом можно перейти на реальный Telegram.

