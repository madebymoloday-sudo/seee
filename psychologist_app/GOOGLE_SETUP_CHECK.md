# ✅ Проверка настройки Google Service Account

## Проверка JSON ключа

Ваш JSON ключ проверен и выглядит корректно:

✅ **Структура валидна:**
- `type`: "service_account" ✓
- `project_id`: "seee-app" ✓
- `client_email`: "seee-crm-service@seee-app.iam.gserviceaccount.com" ✓
- `private_key`: присутствует ✓
- Все необходимые поля на месте ✓

---

## ✅ Что уже сделано в коде:

1. ✅ Создан модуль `crm_integration.py` для работы с Google Sheets
2. ✅ Добавлена библиотека `gspread==5.12.0` в `requirements.txt`
3. ✅ Обновлен `subscription_system.py` для использования CRM
4. ✅ Добавлена защита в `.gitignore` (JSON файлы не попадут в git)

---

## 📋 Что нужно сделать сейчас:

### 1. Добавить переменные в Railway

**Откройте:** `psychologist_app/RAILWAY_SETUP.md` - там подробная инструкция

**Кратко:**
1. Railway → Variables → + New Variable
2. **Key:** `GOOGLE_CREDENTIALS_JSON`
3. **Value:** весь содержимый вашего JSON файла (скопируйте весь файл)
4. **Key:** `GOOGLE_SHEET_ID`
5. **Value:** ID из URL вашей Google Sheets таблицы

### 2. Настроить доступ к таблице

1. Откройте вашу Google Sheets таблицу
2. Нажмите **"Поделиться"**
3. Введите email: `seee-crm-service@seee-app.iam.gserviceaccount.com`
4. Дайте права **"Редактор"**
5. Снимите галочку "Уведомить людей"
6. Нажмите **"Отправить"**

### 3. Создать вкладку "SEEE CRM" (опционально)

Если вкладки еще нет, она создастся автоматически при первом обращении.
Или создайте вручную с заголовками:
- ID пользователя | Имя | Telegram | Email | Количество сессий | Деньги с подписки | Рефералы (количество) | Деньги с рефералов | К выплате | Активный промокод

---

## 🔍 Как проверить, что всё работает:

1. **Перезапустите приложение в Railway**
2. **Попробуйте применить промокод** - данные должны появиться в CRM
3. **Проверьте Google Sheets таблицу** - должна появиться новая строка

---

## ⚠️ Важно!

- **НЕ коммитьте JSON файл в git!** (уже защищено в .gitignore)
- Храните ключ только в Railway Variables
- Если ключ попал в git - удалите его и создайте новый Service Account

---

## 📝 Файлы для справки:

- `RAILWAY_SETUP.md` - инструкция по настройке Railway
- `CRM_TABLE_SETUP.md` - структура CRM таблицы
- `crm_integration.py` - код интеграции с Google Sheets

