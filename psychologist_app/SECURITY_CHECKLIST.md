# Чеклист безопасности для исправления утечки сессий

## ✅ Что нужно проверить и исправить

### 1. Endpoint `/api/sessions` (GET)
- [ ] Фильтрует сессии по `user_id` текущего пользователя
- [ ] Возвращает только сессии текущего пользователя
- [ ] Не возвращает сессии других пользователей

### 2. Endpoint `/api/sessions` (POST)
- [ ] Создает сессию с привязкой к `user_id` текущего пользователя
- [ ] Не позволяет указать чужой `user_id`

### 3. Endpoint `/api/sessions/<session_id>` (GET)
- [ ] Проверяет принадлежность сессии перед возвратом
- [ ] Возвращает 403, если сессия принадлежит другому пользователю

### 4. Endpoint `/api/sessions/<session_id>` (PUT)
- [ ] Проверяет принадлежность сессии перед обновлением
- [ ] Возвращает 403, если сессия принадлежит другому пользователю

### 5. Endpoint `/api/sessions/<session_id>` (DELETE)
- [ ] Проверяет принадлежность сессии перед удалением
- [ ] Возвращает 403, если сессия принадлежит другому пользователю

### 6. Endpoint `/api/sessions/<session_id>/messages` (GET)
- [ ] Проверяет принадлежность сессии перед возвратом сообщений
- [ ] Возвращает 403, если сессия принадлежит другому пользователю

### 7. Endpoint `/api/sessions/<session_id>/document` (GET)
- [ ] Проверяет принадлежность сессии перед возвратом документа
- [ ] Возвращает 403, если сессия принадлежит другому пользователю

### 8. Endpoint `/api/sessions/<session_id>/add-to-map` (POST)
- [ ] Проверяет принадлежность сессии перед добавлением в карту
- [ ] Возвращает 403, если сессия принадлежит другому пользователю

### 9. Socket.IO события
- [ ] `go_to_belief` - проверяет принадлежность сессии
- [ ] `message` - проверяет принадлежность сессии
- [ ] `skip_step` - проверяет принадлежность сессии
- [ ] Все остальные события, работающие с сессиями

### 10. Функции работы с сессиями
- [ ] `get_session()` - проверяет принадлежность
- [ ] `save_session()` - проверяет принадлежность
- [ ] `get_current_concept()` - проверяет принадлежность

---

## 🔍 Как проверить

1. **Создайте двух тестовых пользователей:**
   - Пользователь A
   - Пользователь B

2. **Создайте сессии:**
   - Пользователь A создает сессию "Сессия A"
   - Пользователь B создает сессию "Сессия B"

3. **Проверьте доступ:**
   - Пользователь A НЕ должен видеть "Сессия B"
   - Пользователь B НЕ должен видеть "Сессия A"
   - Попытка открыть чужую сессию должна вернуть 403

4. **Проверьте все endpoints:**
   - GET `/api/sessions` - только свои сессии
   - GET `/api/sessions/<id>` - только свои сессии
   - PUT `/api/sessions/<id>` - только свои сессии
   - DELETE `/api/sessions/<id>` - только свои сессии

---

## ⚠️ Критичность

**ВЫСОКАЯ** - это утечка персональных данных. Исправьте немедленно!

---

## 📝 После исправления

1. Протестируйте все endpoints
2. Убедитесь, что пользователи видят только свои данные
3. Проверьте логи на наличие попыток доступа к чужим данным
4. Обновите документацию по безопасности

