# Интеграция иерархического отображения

## ✅ Реализовано

Добавлено иерархическое отображение идей в списке убеждений с визуальными отступами, цветными границами и возможностью раскрытия/сворачивания веток.

---

## Как это выглядит

### Визуальное представление:

```
▶ Желание реализовать свой гений          [синяя граница]
  ▼ из желания реализовать свой гений     [зеленая граница, отступ 20px]
    ▶ способность создавать сильный ИИ   [желтая граница, отступ 40px]
    ▶ способность разрабатывать сильный ИИ [желтая граница, отступ 40px]
  ▶ гений                                 [зеленая граница, отступ 20px]
```

### Особенности:

1. **Иконки раскрытия:** ▶ (свернуто) / ▼ (развернуто)
2. **Отступы:** 20px на каждый уровень
3. **Цветные границы:** Разные цвета для разных уровней
4. **Вертикальная линия:** Показывает связь родитель-ребенок

---

## Интеграция

### Backend

Добавьте в ваш `app.py`:

1. **Функцию построения иерархии:**
```python
def build_concepts_hierarchy(concepts):
    # Код из app_template.py (строки 267-320)
```

2. **Обработчик Socket.IO:**
```python
@socketio.on('get_concepts_hierarchy')
def handle_get_concepts_hierarchy(data):
    # Код из app_template.py (строки 240-254)
```

### Frontend

Все изменения уже добавлены в:
- ✅ `static/js/app.js` - функции построения и отображения иерархии
- ✅ `static/css/style.css` - стили для иерархии

---

## Автоматическая работа

Система автоматически:
1. Запрашивает иерархию при открытии модального окна выбора убеждений
2. Отображает иерархию вместо плоского списка
3. Обновляет иерархию после изменений (создание, удаление, переименование)

---

## Структура данных

Иерархия строится на основе поля `extracted_from`:

```json
{
  "Желание реализовать свой гений": {
    "extracted_from": null  // Корневая идея
  },
  "из желания реализовать свой гений": {
    "extracted_from": "Желание реализовать свой гений"  // Дочерняя идея
  }
}
```

---

## Готово!

После интеграции backend изменений список убеждений будет автоматически отображать иерархию! 🎉

