# 📋 Руководство по внедрению изменений

## 🎯 Обзор

Все изменения на **frontend** полностью готовы и работают. Для полной функциональности нужно интегрировать изменения в **backend**.

---

## ✅ Что уже готово (Frontend)

### Все 13 задач реализованы на frontend:

1. ✅ Редактирование/удаление идей
2. ✅ Кнопка "Далее" (было "Пропустить")
3. ✅ Копирование сообщений
4. ✅ Enter для переноса строки
5. ✅ Поле ввода с прокруткой (8 строк)
6. ✅ Настройка масштаба текста
7. ✅ Зачеркивание идей
8. ✅ Просмотр идеи целиком
9. ✅ Открытие боковой панели (одно касание)
10. ✅ Кнопка боковой панели в левом верхнем углу

### Требуют backend (3 задачи):

- ⚠️ Задача 3: Разделение последствий (вопросы)
- ⚠️ Задача 7: Изменить вопрос после частей
- ⚠️ Задача 8: Изменить вопрос о цели

---

## 🚀 Быстрый старт

### 1. Проверьте файлы

Убедитесь, что у вас есть:
- ✅ `psychologist_app/static/js/app.js` (обновлен)
- ✅ `psychologist_app/static/css/style.css` (обновлен)
- ✅ `psychologist_app/app_template.py` (новый - шаблон для backend)
- ✅ `psychologist_app/BACKEND_CHANGES_NEEDED.md` (инструкции)

### 2. Интегрируйте backend изменения

Откройте `app_template.py` и скопируйте нужные части в ваш `app.py`:

#### Минимальные изменения (только вопросы):

```python
# Задача 7: Изменить вопрос после частей
if current_field == 'parts' and is_complete:
    question = "Есть ли ещё какие-то части этой идеи или идём дальше?"

# Задача 8: Изменить вопрос о цели
if current_field == 'goal' or current_field == 'purpose':
    question = "Как вы думаете с какой целью эта идея внедрялась в ваш разум?"

# Задача 3: Разделение последствий
if current_field == 'consequences_emotional':
    question = "Какие эмоциональные последствия имеет эта идея?"
elif current_field == 'consequences_physical':
    question = "Какие физические последствия имеет эта идея?"
```

#### Полная интеграция (все новые функции):

Скопируйте обработчики Socket.IO из `app_template.py`:
- `update_belief_name`
- `delete_belief`
- `strikethrough_belief`
- `get_concept_full`
- `extract_concept_part`

---

## 📝 Детальные инструкции

### Задача 3: Разделение последствий

**Текущая структура данных уже поддерживает:**
```json
"consequences": {
    "emotional": [],
    "physical": []
}
```

**Что нужно изменить:**
1. Вместо одного вопроса о последствиях задавать два вопроса
2. Сохранять ответы в соответствующие массивы

**Пример кода:**
```python
# Определяем порядок полей
field_order = ['goal', 'parts', 'founder', 'consequences_emotional', 'consequences_physical', 'conclusion']

# При переходе к последствиям
if next_field == 'consequences_emotional':
    question = "Какие эмоциональные последствия имеет эта идея?"
elif next_field == 'consequences_physical':
    question = "Какие физические последствия имеет эта идея?"
```

---

### Задача 7: Вопрос после частей

**Было:**
```python
question = "Есть ли что-то ещё что вы хотели бы изменить или добавить к этой идее?"
if skip_step:
    response = "Хорошо, пропустим состав"
```

**Стало:**
```python
question = "Есть ли ещё какие-то части этой идеи или идём дальше?"
if skip_step:
    # Просто переходим к следующему полю, без сообщения
    move_to_next_field()
```

---

### Задача 8: Вопрос о цели

**Было:**
```python
question = "С какой целью появилась идея?"
```

**Стало:**
```python
question = "Как вы думаете с какой целью эта идея внедрялась в ваш разум?"
```

---

## 🔧 Новые Socket.IO события

Все события уже обрабатываются на frontend. Нужно добавить обработчики на backend:

### 1. `update_belief_name`
Обновляет название убеждения.

**Параметры:**
- `session_id`
- `old_name`
- `new_name`

**Ответ:**
```python
emit('response', {
    'message': f'Название обновлено на "{new_name}"',
    'available_concepts': list(concepts.keys())
})
```

### 2. `delete_belief`
Удаляет убеждение.

**Параметры:**
- `session_id`
- `concept_name`

### 3. `strikethrough_belief`
Помечает убеждение как неактуальное.

**Параметры:**
- `session_id`
- `concept_name`

**Действие:**
```python
concept['is_strikethrough'] = True
```

### 4. `get_concept_full`
Возвращает полную структуру концепции.

**Параметры:**
- `session_id`
- `concept_name`

**Ответ:**
```python
emit('concept_full_structure', {
    'concept_name': concept_name,
    'structure': {
        'goal': concept.get('purpose'),
        'parts': concept.get('composition', []),
        'founder': concept.get('founder'),
        'consequences_emotional': concept.get('consequences', {}).get('emotional', []),
        'consequences_physical': concept.get('consequences', {}).get('physical', []),
        'conclusion': concept.get('conclusions')
    }
})
```

### 5. `extract_concept_part`
Извлекает часть концепции как новую идею.

**Параметры:**
- `session_id`
- `source_concept`
- `part_type`
- `part_value`

---

## 🧪 Тестирование

После интеграции проверьте:

1. ✅ Редактирование убеждений работает
2. ✅ Удаление убеждений работает
3. ✅ Просмотр структуры идеи работает
4. ✅ Извлечение частей работает
5. ✅ Зачеркивание работает
6. ✅ Вопросы изменены правильно
7. ✅ Обработка "Далее" работает корректно

---

## 📞 Поддержка

Если возникнут вопросы:
1. Проверьте `app_template.py` - там полные примеры
2. Проверьте `BACKEND_CHANGES_NEEDED.md` - там детальные инструкции
3. Проверьте консоль браузера на ошибки JavaScript
4. Проверьте логи сервера на ошибки Python

---

## ✨ Готово!

После интеграции всех изменений все 13 задач будут полностью реализованы! 🎉

