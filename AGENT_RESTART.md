# 🔄 Автоматический перезапуск агента

## Что исправлено

### 1. ✅ Исправлен баг в `updater.py`
- Удален недостижимый код после `return`
- Логика Git коммита теперь выполняется корректно

### 2. ✅ Добавлен автоматический перезапуск агента
- После применения исправлений кода создается флаг `/tmp/agent_restart_flag`
- Главный процесс мониторит этот флаг каждую секунду
- При обнаружении флага агент автоматически перезапускается

### 3. ✅ Улучшено логирование
- Все логи теперь отправляются в веб-интерфейс
- Добавлено подробное логирование в `run_improvement_in_thread`
- PID процесса сохраняется в `/tmp/agent_improvement.pid`

## Как это работает

1. **Применение исправлений:**
   - Агент применяет исправления к коду
   - Создается флаг `/tmp/agent_restart_flag`
   - Отправляется уведомление в веб-интерфейс

2. **Автоматический перезапуск:**
   - Главный процесс обнаруживает флаг
   - Запускает новый процесс `run_with_web.py`
   - Завершает текущий процесс
   - Новый процесс загружает обновленный код

3. **Логирование:**
   - Все сообщения отправляются в веб-интерфейс через `web_chat_viewer`
   - Логи также выводятся в консоль
   - PID процесса сохраняется для отслеживания

## Файлы

- `/tmp/agent_improvement.pid` - PID текущего процесса агента
- `/tmp/agent_restart_flag` - флаг для перезапуска (создается автоматически)
- `restart_agent.py` - скрипт для ручного перезапуска (опционально)

## Проверка работы

1. Откройте http://localhost:5001 в браузере
2. Дождитесь, пока агент найдет ошибки и применит исправления
3. После применения исправлений агент автоматически перезапустится
4. В веб-интерфейсе вы увидите сообщение "🔄 Обнаружены обновления кода, перезапускаю агента..."

## Ручной перезапуск

Если нужно перезапустить агента вручную:

```bash
python3 restart_agent.py restart
```

Или создайте флаг вручную:

```bash
touch /tmp/agent_restart_flag
```



