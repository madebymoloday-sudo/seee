# Инструкция по отправке кода в GitHub

## ✅ Что уже сделано:

1. ✅ Удалены все локальные файлы (run_local.py, LOCAL_SETUP.md и т.д.)
2. ✅ Исправлена структура репозитория (удален вложенный .git из psychologist_app)
3. ✅ Все изменения закоммичены в локальный репозиторий

## 📤 Следующий шаг - Отправка в GitHub:

### Вариант 1: Использовать готовый скрипт

```bash
cd /Users/pavelgulo/Desktop/курсор
bash push_to_github.sh
```

Скрипт спросит URL вашего GitHub репозитория, если он еще не настроен.

### Вариант 2: Настроить вручную

Если у вас уже есть GitHub репозиторий:

```bash
cd /Users/pavelgulo/Desktop/курсор

# Добавить remote (замените YOUR_USERNAME и REPO_NAME на ваши)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git

# Отправить код
git push -u origin main
```

### Вариант 3: Создать новый репозиторий

Если репозитория еще нет:

1. Откройте https://github.com/new
2. Создайте новый репозиторий (например, `seee-app` или `psychologist-app`)
3. НЕ добавляйте README, .gitignore, license
4. Скопируйте URL репозитория
5. Выполните:

```bash
cd /Users/pavelgulo/Desktop/курсор
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

## 🚀 После отправки в GitHub:

Railway автоматически получит обновления через GitHub webhook и обновит деплой.

Если автоматический деплой не сработал:
1. Откройте Railway dashboard
2. Найдите ваш проект
3. Нажмите "Redeploy" или "Deploy"

## 📋 Что включено в коммит:

- ✅ Все API endpoints для личного кабинета
- ✅ Исправления UI (центрирование, боковая панель, поиск)
- ✅ Обнаружение суицидальных мыслей
- ✅ Система подписки и промокодов
- ✅ Интеграция с CRM (Google Sheets)
- ✅ Все JavaScript файлы с исправлениями
- ✅ Обновленный CSS
- ✅ Все необходимые таблицы БД

