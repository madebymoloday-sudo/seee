# ⚡ Быстрый деплой

## 🎯 Самый простой способ: Railway

1. **Откройте:** https://railway.app
2. **Войдите через GitHub** (если есть подписка)
3. **New Project** → **Deploy from GitHub repo**
4. **Выберите репозиторий** (или создайте новый)
5. **Railway автоматически запустит приложение!**

### Настройка переменных:
В настройках проекта добавьте:
- `SECRET_KEY` = любой случайный ключ
- `OPENAI_API_KEY` = ваш ключ из .env

**Готово!** Railway даст вам публичный URL типа: `https://your-app.railway.app`

---

## 🔧 Альтернатива: Ngrok (локальный сервер)

```bash
# 1. Получите токен: https://dashboard.ngrok.com/get-started/your-authtoken
# 2. Запустите:
./setup_ngrok.sh
```

Или вручную:
```bash
ngrok config add-authtoken ВАШ_ТОКЕН
ngrok http 5003
```

---

## 📋 Что уже готово:
✅ `Procfile` - для запуска
✅ `requirements.txt` - зависимости  
✅ `railway.json` - конфигурация
✅ Порт из переменной окружения (PORT)
✅ `.gitignore` - исключения для git

**Подробнее:** см. DEPLOY.md
