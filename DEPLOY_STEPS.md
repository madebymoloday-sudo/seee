# 🚀 Пошаговая инструкция деплоя

## ✅ Шаг 1: Git репозиторий готов!

Все файлы закоммичены. Теперь нужно:

---

## 📦 Шаг 2: Создать репозиторий на GitHub

### Вариант A: Через веб-интерфейс (проще)

1. Откройте: https://github.com/new
2. **Repository name:** `psychologist-app`
3. **Visibility:** Public или Private (на ваш выбор)
4. **НЕ добавляйте** README, .gitignore, license (у нас уже есть)
5. Нажмите **Create repository**

### Вариант B: Через GitHub CLI (если установлен)

```bash
gh repo create psychologist-app --public --source=. --remote=origin --push
```

---

## 🔗 Шаг 3: Подключить к GitHub

Выполните в терминале:

```bash
cd /Users/pavelgulo/Desktop/курсор/psychologist_app

# Если репозиторий создан через веб:
git remote add origin https://github.com/ВАШ_USERNAME/psychologist-app.git
git branch -M main
git push -u origin main

# Если через GitHub CLI - уже сделано автоматически
```

---

## 🚂 Шаг 4: Деплой на Railway

1. **Откройте:** https://railway.app
2. **Войдите через GitHub** (если есть подписка)
3. **New Project** → **Deploy from GitHub repo**
4. **Выберите репозиторий:** `psychologist-app`
5. Railway автоматически определит Python и начнет деплой!

---

## ⚙️ Шаг 5: Настройка переменных окружения

В Railway (Settings → Variables) добавьте:

```
SECRET_KEY = сгенерируйте_случайный_ключ_минимум_32_символа
OPENAI_API_KEY = ваш_ключ_из_файла_.env
PORT = (Railway установит автоматически, но можно указать 5003)
```

### Как сгенерировать SECRET_KEY:
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

---

## ✅ Шаг 6: Готово!

Railway даст вам публичный URL типа: `https://psychologist-app.railway.app`

**Этот URL работает из любой сети!**

---

## 🔍 Проверка деплоя

После деплоя проверьте:
- ✅ Сайт открывается по публичному URL
- ✅ Регистрация работает
- ✅ Чат с AI работает
- ✅ База данных создается

---

## 📝 Логи

В Railway можно смотреть логи в реальном времени:
- Откройте проект → View Logs

---

## 🐛 Если что-то не работает

1. Проверьте логи в Railway
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что порт правильно настроен (Railway автоматически устанавливает PORT)





