# 🤖 GPT Интеграция для тестового клиента

## 📋 Что добавлено

Тестовый клиент теперь может генерировать **реалистичные сообщения клиента** через GPT API!

### Как это работает:

1. **Тестовый клиент (второй бот)** теперь использует GPT для генерации вопросов клиента
2. GPT имитирует поведение девушки с типичными проблемами со здоровьем
3. Сообщения генерируются **динамически** на основе ответов бота
4. Это создает более **реалистичный диалог** для тестирования

## 🔧 Настройка

### Способ 1: Использовать API ключ из проекта Instagram бота

Если у вас уже есть OpenAI API ключ из проекта Instagram бота:

1. Добавьте в файл `.env`:
```env
OPENAI_API_KEY=ваш_ключ_от_instagram_бота
```

Система автоматически использует его!

### Способ 2: Указать отдельный ключ

```env
AI_API_KEY=ваш_openai_api_ключ
AI_MODEL=gpt-4o-mini  # или gpt-4
AI_TEMPERATURE=0.7
```

### Способ 3: Использовать переменную окружения напрямую

Система проверяет переменные окружения в следующем порядке:
1. `AI_API_KEY`
2. `OPENAI_API_KEY` (из Instagram бота)

## 🎯 Что генерирует GPT

GPT имитирует клиента с проблемами:
- 🍎 Похудение (вес возвращается после диет)
- 🌸 Проблемы с кожей (высыпания, воспаления, жирная кожа)
- 💇 Проблемы с волосами (выпадение, тусклость)
- 🦵 Целлюлит
- 😴 Усталость и недостаток энергии
- 🩺 Гормональные проблемы
- 💊 Общее самочувствие

## 🚀 Использование

### Запуск с GPT (автоматически):

```bash
python run_with_chat.py
```

Если GPT доступен, система автоматически:
- ✅ Использует GPT для генерации сообщений
- ✅ Создает динамические диалоги
- ✅ Адаптирует вопросы на основе ответов бота

### Если GPT недоступен:

Система автоматически переключится на статические сценарии.

## 📊 Режимы работы

1. **GPT режим** (если API ключ есть):
   - Генерирует реалистичные вопросы через GPT
   - Адаптирует вопросы на основе ответов бота
   - Создает более естественные диалоги

2. **Статический режим** (если GPT недоступен):
   - Использует предустановленные сценарии
   - Все равно тестирует основные проблемы

## 💡 Преимущества GPT режима

- ✨ Более реалистичные диалоги
- 🎯 Разнообразие вопросов
- 🔄 Адаптация к ответам бота
- 📈 Лучшее тестирование граничных случаев

## ⚙️ Настройки генерации

В `config/settings.py` можно настроить:

```python
AI_MODEL = "gpt-4o-mini"  # или "gpt-4"
AI_TEMPERATURE = 0.7      # Креативность (0.0-1.0)
```

## 🔍 Пример работы

```
👤 Клиент (GPT): Привет! У меня проблема с кожей лица
🤖 Бот: Проблемы с кожей часто связаны с питанием...

👤 Клиент (GPT): А что если проблема в гормонах?
🤖 Бот: Гормональные проблемы могут влиять...

👤 Клиент (GPT): Можете посоветовать что-то конкретное?
🤖 Бот: ...
```

Каждое сообщение клиента генерируется динамически на основе предыдущих ответов!

## 📝 Логирование

Все сгенерированные через GPT сообщения логируются в:
- `logs/conversations.jsonl` - все диалоги
- Система отслеживает, какие сообщения были сгенерированы через GPT

## ⚠️ Важно

- GPT генерация может быть медленнее статических сценариев
- Для GPT нужен валидный OpenAI API ключ
- Система автоматически fallback на статические сценарии, если GPT недоступен

