# Настройка Webhook для автоматического обновления

## Как это работает

1. **Агент находит ошибки** → анализирует их → применяет исправления
2. **Автоматический коммит** → изменения автоматически коммитятся в Git
3. **Автоматический push** → изменения отправляются в GitHub
4. **GitHub Webhook** → Railway получает уведомление о push
5. **Автоматический деплой** → Railway автоматически обновляет приложение

## Эндпоинты Webhook

### 1. `/webhook/github` (POST)
Получает уведомления от GitHub о push событиях.

**Настройка на GitHub:**
1. Зайдите в настройки вашего репозитория на GitHub
2. Settings → Webhooks → Add webhook
3. Payload URL: `https://ваш-домен.railway.app/webhook/github`
4. Content type: `application/json`
5. Events: выберите `Just the push event`
6. Active: ✓

### 2. `/webhook/update` (POST)
Вызывается агентом для инициирования обновления.

**Формат запроса:**
```json
{
  "message": "Агент запросил обновление кода",
  "details": "Применено 3 исправления"
}
```

## Настройка автоматического коммита и push

Система автоматически:
- ✅ Коммитит изменения после применения исправлений
- ✅ Отправляет в GitHub (если remote настроен)
- ✅ Создает информативные сообщения коммитов

**Требования:**
- Git репозиторий должен быть инициализирован
- Remote `origin` должен быть настроен (для push)
- Права на запись в репозиторий

## Непрерывный цикл тестирования

Система работает в непрерывном цикле:

1. **Тестирование** → боты общаются
2. **Мониторинг ошибок** → система отслеживает проблемы
3. **Анализ** → агент анализирует ошибки
4. **Исправление** → агент применяет исправления
5. **Коммит и push** → изменения отправляются в Git
6. **Деплой** → Railway автоматически обновляет приложение
7. **Перезапуск** → бот перезапускается с новым кодом
8. **Повтор цикла** → процесс повторяется

## Проверка работы

1. Откройте веб-интерфейс: `http://localhost:5001`
2. Следите за уведомлениями агента в правой панели
3. Когда агент применит исправления, вы увидите:
   - "💻 Применение кода"
   - "✅ Исправления применены!"
   - "📤 Изменения отправлены в Git"

4. Проверьте GitHub репозиторий - должны появиться коммиты с сообщениями:
   - "🤖 Авто-исправление: применено X исправлений"

5. Проверьте Railway - должен начаться новый деплой

## Настройка переменных окружения на Railway

Убедитесь, что на Railway настроены:
- `PORT` - автоматически устанавливается Railway
- Все остальные переменные из `config/settings.py`

## Отладка

Если автоматический коммит не работает:
1. Проверьте логи: `[CodeUpdater]` в консоли
2. Убедитесь, что Git репозиторий настроен
3. Проверьте права доступа к репозиторию
4. Проверьте, что remote `origin` настроен: `git remote -v`

Если webhook не работает:
1. Проверьте URL webhook на GitHub
2. Проверьте логи на Railway
3. Убедитесь, что endpoint доступен публично

