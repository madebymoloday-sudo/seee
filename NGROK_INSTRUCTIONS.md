# 🌐 Доступ к сайту из другой сети через ngrok

## Быстрый старт

1. **Убедитесь, что сервер запущен:**
   ```bash
   cd /Users/pavelgulo/Desktop/курсор/psychologist_app
   python3 run.py
   ```

2. **В новом окне терминала запустите ngrok:**
   ```bash
   ngrok http 5003
   ```

3. **Скопируйте публичный URL:**
   - В терминале ngrok вы увидите строку вида:
     ```
     Forwarding   https://xxxx-xx-xx-xx-xx.ngrok-free.app -> http://localhost:5003
     ```
   - Или откройте в браузере: http://127.0.0.1:4040
   - Скопируйте HTTPS URL (начинается с `https://`)

4. **Отправьте этот URL коллегам** - они смогут открыть сайт из любой сети!

## Альтернатива: автоматический запуск

Запустите скрипт:
```bash
cd /Users/pavelgulo/Desktop/курсор/psychologist_app
./start_ngrok.sh
```

## Важно

- ngrok создает **временный URL**, который меняется при каждом перезапуске (если не используете платный аккаунт)
- Для постоянного URL нужен платный ngrok аккаунт или другой хостинг
- Публичный URL работает из **любой сети** (не только Wi-Fi)

## Проверка статуса

- Веб-интерфейс ngrok: http://127.0.0.1:4040
- Логи ngrok: `/tmp/ngrok_output.log`




