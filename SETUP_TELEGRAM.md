# 📱 Настройка Telegram для работы ботов

## 🎯 Два варианта запуска:

### ✅ Вариант 1: Эмулятор (рекомендуется для начала)

**Ничего не нужно делать в Telegram!** Система работает локально на вашем компьютере.

Просто запустите:
```bash
python main.py --mode single
```

Это самый простой способ начать - всё работает сразу без настройки!

---

### 🔵 Вариант 2: Реальный Telegram (для продакшена)

Если хотите тестировать на реальном Telegram, выполните следующие шаги:

## Шаг 1: Создать двух ботов через @BotFather

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Придумайте имя для **основного бота** (например: "MyTestBot")
   - Придумайте username (должен заканчиваться на `bot`, например: `mytestbot`)
   - **Скопируйте токен** (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

4. Создайте **второго бота** (тестовый бот):
   - Отправьте `/newbot` снова
   - Имя: "MyTestBotClient"
   - Username: `mytestbotclient`
   - **Скопируйте второй токен**

## Шаг 2: Создать тестовую группу

1. Создайте новую группу в Telegram
2. Добавьте туда **оба бота**:
   - Нажмите "Добавить участников"
   - Найдите своих ботов по username
   - Добавьте их в группу
3. Сделайте ботов администраторами группы (опционально)

## Шаг 3: Получить chat_id группы

Есть несколько способов:

### Способ 1: Через @userinfobot
1. Добавьте бота @userinfobot в вашу группу
2. Он покажет chat_id группы

### Способ 2: Через API
1. Отправьте сообщение в группу от любого из ботов
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
   Замените `<ВАШ_ТОКЕН>` на токен одного из ботов
3. Найдите `"chat":{"id":-123456789}` - это chat_id группы (отрицательное число)

### Способ 3: Программа автоматически
Система может попробовать найти chat_id автоматически при запуске

## Шаг 4: Настроить .env файл

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Tokens
MAIN_BOT_TOKEN=ваш_токен_основного_бота
TEST_BOT_TOKEN=ваш_токен_тестового_бота

# Настройки
TEST_GROUP_ID=-123456789  # ID вашей группы (отрицательное число)

# Режим работы
USE_REAL_TELEGRAM=true    # Включить реальный Telegram
EMULATOR_MODE=false       # Отключить эмулятор

# AI Agent (опционально)
AI_API_KEY=ваш_openai_api_ключ  # Если хотите AI-исправления
AI_MODEL=gpt-4
```

## Шаг 5: Отправить данные

Отправьте мне:
1. ✅ `MAIN_BOT_TOKEN` - токен основного бота
2. ✅ `TEST_BOT_TOKEN` - токен тестового бота  
3. ✅ `TEST_GROUP_ID` - ID группы (или я помогу его найти)

**ВНИМАНИЕ:** ⚠️ Не публикуйте токены в публичных местах! Токены дают полный доступ к ботам.

## Шаг 6: Запустить систему

```bash
python main.py --mode continuous --cycles 5
```

---

## 🔒 Безопасность

- ✅ **НЕ коммитьте** файл `.env` в git
- ✅ **НЕ публикуйте** токены
- ✅ Файл `.env` уже добавлен в `.gitignore`

---

## ❓ Что мне нужно от вас:

### Для эмулятора (простой вариант):
**Ничего!** Просто скажите "запусти эмулятор" и я запущу систему локально.

### Для реального Telegram:
Отправьте мне:
1. Токен основного бота
2. Токен тестового бота
3. Chat ID группы (или я помогу найти)

---

## 🚀 Быстрый старт

Для начала рекомендую **эмулятор** - он работает сразу без настройки Telegram!

