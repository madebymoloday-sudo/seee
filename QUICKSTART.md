# 🚀 Быстрый старт

## Установка

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Создайте файл `.env`** (опционально, для AI-интеграции):
```bash
# .env
AI_API_KEY=your_openai_api_key_here  # Опционально, для AI-исправлений
AI_MODEL=gpt-4
```

## Запуск

### Первый запуск - одноразовое тестирование:
```bash
python main.py --mode single
```

Это запустит:
1. Основной бот в режиме эмуляции
2. Набор тестовых сценариев
3. Отчет о найденных ошибках

### Непрерывное улучшение:
```bash
python main.py --mode continuous --cycles 5 --delay 5
```

Это запустит автоматический цикл:
1. Тестирование
2. Анализ ошибок
3. Генерация исправлений (через AI если указан API ключ, иначе простые исправления)
4. Обновление кода бота
5. Повтор цикла

**Параметры:**
- `--mode`: `single` или `continuous`
- `--cycles`: количество циклов улучшения (по умолчанию 5)
- `--delay`: задержка между циклами в секундах (по умолчанию 5)

## Что происходит при каждом цикле:

1. ✅ Запускается основной бот
2. ✅ Тестовый клиент отправляет набор сообщений
3. ✅ Система проверяет ответы на ошибки
4. ✅ Все диалоги логируются в `logs/`
5. ✅ Агент анализирует ошибки
6. ✅ Генерируются исправления кода
7. ✅ Код бота автоматически обновляется
8. ✅ Создается бэкап в `backup/`

## Файлы и директории:

- `main_bot/bot.py` - основной бот (который улучшается)
- `logs/` - логи всех диалогов и ошибок
- `backup/` - резервные копии кода перед обновлениями
- `test_client/tester.py` - тестовые сценарии (можно редактировать)

## Пример вывода:

```
🚀 ЗАПУСК СИСТЕМЫ АВТОМАТИЧЕСКОГО САМОСОВЕРШЕНСТВОВАНИЯ
   Максимум циклов: 5
   Задержка между циклами: 5 сек

============================================================
ЦИКЛ #1/5 - 12:00:00
============================================================

[1/5] Запуск основного бота...
[MainBot] Бот запущен в режиме эмуляции

[2/5] Запуск тестирования...
[TestClient] Отправка: Привет
[Emulator] test_client -> test_chat_1: Привет
[Emulator] main_bot -> test_chat_1: Привет! Как дела?
[TestClient] Отчет о тестировании:
  Тестов пройдено: 15
  Ошибок найдено: 3
  Процент ошибок: 20.00%

[3/5] Логирование диалогов...

[4/5] Анализ ошибок и генерация исправлений...

[5/5] Применение исправлений...
[Updater] Функция _generate_response обновлена в main_bot/bot.py
✅ Применено исправлений: 1

⚠️  ВНИМАНИЕ: Код бота был обновлен!
   Перезапустите бота для применения изменений.
```

## Важно:

- ⚠️ Система автоматически изменяет код! Всегда проверяйте изменения перед деплоем.
- 📁 Все бэкапы сохраняются в `backup/`
- 📊 Все логи сохраняются в `logs/`

