# Тест-агент для AI-психолога

AI-агент для автоматического тестирования веб-приложения AI-психолога через браузер.

## Возможности

- 🤖 Автоматическое управление браузером через Selenium
- 🔐 Автоматический вход/регистрация в системе
- 💬 Ведение диалога с AI-психологом
- 📊 Отслеживание ошибок и проблем
- 📝 Отправка логов в чат (через web_chat_viewer)
- 🧪 Запуск различных тестовых сценариев

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Убедитесь, что у вас установлен Chrome и ChromeDriver:
```bash
# macOS
brew install chromedriver

# Или скачайте с https://chromedriver.chromium.org/
```

3. Убедитесь, что приложение AI-психолога запущено:
```bash
cd psychologist_app
python3 run.py
```

## Использование

### Базовый запуск

```bash
cd psychologist_app
python3 run_test_agent.py
```

### Параметры

Агент можно настроить в коде:

```python
agent = PsychologistTestAgent(
    base_url="http://localhost:5003",  # URL приложения
    headless=False  # Показывать браузер (True = скрытый режим)
)
```

## Тестовые сценарии

Агент автоматически запускает следующие сценарии:

1. **Проверка начального сообщения** - проверяет приветствие
2. **Простой ответ о самочувствии** - тестирует обработку "плохо"
3. **Позитивный ответ** - тестирует обработку "хорошо"
4. **Описание эмоции** - проверяет распознавание эмоций
5. **Описание ситуации** - проверяет переход к ситуациям
6. **Полный диалог** - комплексный тест

## Проверяемые ошибки

Агент автоматически обнаруживает:

- ❌ Пустые ответы от AI
- ❌ Сообщения об ошибках
- ❌ Дублирование вопросов
- ❌ Некорректные вопросы (два вопроса одновременно)

## Логирование

Все логи отправляются:
- 📺 В консоль (stdout)
- 💬 В web_chat_viewer (если доступен)
- 📝 В `conversation_log` для последующего анализа

## Пример вывода

```
[2025-12-07 22:50:00] [INFO] ✅ WebDriver успешно инициализирован
[2025-12-07 22:50:02] [INFO] 🔐 Начинаю процесс входа...
[2025-12-07 22:50:05] [INFO] ✅ Успешный вход в систему
[2025-12-07 22:50:07] [INFO] 💬 Отправляю сообщение: привет
[2025-12-07 22:50:10] [INFO] 🤖 Ответ AI: Здравствуйте! Как вы себя чувствуете?
[2025-12-07 22:50:12] [INFO] ⚠️ Найдена ошибка: AI дублирует вопрос пользователя
```

## Интеграция с web_chat_viewer

Агент автоматически отправляет уведомления в web_chat_viewer (если он запущен):

- 📊 Статус тестирования
- ⚠️ Найденные ошибки
- ✅ Успешные операции
- 📝 Детальные логи

## Расширение функциональности

Вы можете добавить свои тестовые сценарии:

```python
test_scenarios = [
    ["ваше сообщение 1", "ваше сообщение 2"],
    # ...
]
```

Или создать собственные проверки ошибок:

```python
def check_for_errors(self, user_message: str, ai_response: str) -> List[str]:
    errors = []
    # Ваша логика проверки
    return errors
```

## Устранение неполадок

### ChromeDriver не найден

```bash
# Установите ChromeDriver
brew install chromedriver

# Или добавьте в PATH
export PATH=$PATH:/path/to/chromedriver
```

### Элементы не находятся

Проверьте селекторы в коде. Агент использует несколько селекторов для поиска элементов, но может потребоваться адаптация под ваш интерфейс.

### Приложение не отвечает

Убедитесь, что:
1. Приложение запущено на правильном порту (по умолчанию 5003)
2. База данных инициализирована
3. GPT API ключ настроен (если используется)

## Лицензия

Часть проекта AI-психолога.




