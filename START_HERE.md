# 🚀 БЫСТРЫЙ СТАРТ - Доступ из сети

## ✅ Всё готово для деплоя!

---

## 🎯 ВАРИАНТ 1: Railway (Лучший вариант - постоянный URL)

### Шаги (5 минут):

1. **Откройте:** https://railway.app
2. **Войдите через GitHub** (если есть подписка)
3. **New Project** → **Deploy from GitHub repo**
4. **Выберите репозиторий** с этим проектом
5. **Railway автоматически определит Python и запустит!**

### Настройка переменных окружения:

В настройках проекта (Settings → Variables) добавьте:

```
SECRET_KEY = любой_случайный_ключ_минимум_32_символа
OPENAI_API_KEY = ваш_ключ_из_файла_.env
```

### Готово! 
Railway даст вам публичный URL типа: `https://your-app.railway.app`

**Этот URL работает из любой сети и не меняется!**

---

## ⚡ ВАРИАНТ 2: Ngrok (Быстро, но URL меняется)

### Если нужен быстрый доступ прямо сейчас:

1. **Получите токен** (2 минуты):
   - Откройте: https://dashboard.ngrok.com/signup
   - Зарегистрируйтесь (можно через Google)
   - Получите токен: https://dashboard.ngrok.com/get-started/your-authtoken

2. **Настройте ngrok:**
   ```bash
   cd /Users/pavelgulo/Desktop/курсор/psychologist_app
   ./setup_ngrok.sh
   ```
   
   Или вручную:
   ```bash
   ngrok config add-authtoken ВАШ_ТОКЕН
   ngrok http 5003
   ```

3. **Скопируйте публичный URL** из терминала и отправьте коллегам!

---

## 📋 Что уже настроено:

✅ `Procfile` - для запуска на Railway/Heroku  
✅ `railway.json` - конфигурация Railway  
✅ `runtime.txt` - версия Python  
✅ `requirements.txt` - зависимости  
✅ `.gitignore` - исключения для git  
✅ Порт из переменной окружения (PORT)  
✅ Скрипт `setup_ngrok.sh` для ngrok  

---

## 🔍 Проверка локального сервера:

```bash
cd /Users/pavelgulo/Desktop/курсор/psychologist_app
python3 run.py
```

Сервер должен работать на: http://127.0.0.1:5003

---

## ❓ Вопросы?

- Подробная инструкция: `DEPLOY.md`
- Быстрый деплой: `QUICK_DEPLOY.md`
- Настройка ngrok: `NGROK_SETUP.md`

