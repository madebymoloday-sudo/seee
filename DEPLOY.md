# 🚀 Размещение сайта в облаке

## Вариант 1: Railway (Рекомендуется - самый простой)

### Шаги:
1. Зарегистрируйтесь на https://railway.app (можно через GitHub)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Подключите ваш репозиторий (или создайте новый)
4. Railway автоматически определит Python и запустит приложение
5. Получите публичный URL!

### Настройка переменных окружения в Railway:
- `SECRET_KEY` - любой случайный ключ
- `OPENAI_API_KEY` - ваш API ключ OpenAI (из .env файла)

### Файлы уже готовы:
- ✅ `Procfile` - для запуска
- ✅ `requirements.txt` - зависимости
- ✅ `railway.json` - конфигурация

---

## Вариант 2: Render (Бесплатный тариф)

### Шаги:
1. Зарегистрируйтесь на https://render.com
2. New → Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python3 run.py`
5. Добавьте переменные окружения (SECRET_KEY, OPENAI_API_KEY)

---

## Вариант 3: Heroku

### Шаги:
1. Установите Heroku CLI: `brew install heroku/brew/heroku`
2. Войдите: `heroku login`
3. Создайте приложение: `heroku create psychologist-app`
4. Деплой: `git push heroku main`
5. Настройте переменные: `heroku config:set SECRET_KEY=xxx OPENAI_API_KEY=xxx`

---

## Вариант 4: Ngrok (для локального сервера)

### Быстрая настройка:
```bash
# 1. Получите токен: https://dashboard.ngrok.com/get-started/your-authtoken
# 2. Запустите:
./setup_ngrok.sh
# Или вручную:
ngrok config add-authtoken ВАШ_ТОКЕН
ngrok http 5003
```

---

## ⚠️ Важно перед деплоем:

1. **База данных**: SQLite не подходит для продакшена. Нужно:
   - PostgreSQL (Railway/Render предоставляют бесплатно)
   - Или использовать файловую БД (для тестирования)

2. **Переменные окружения**: Не коммитьте `.env` файл!

3. **Порт**: Приложение должно слушать порт из переменной окружения:
   ```python
   port = int(os.environ.get('PORT', 5003))
   ```

---

## 🔧 Быстрый деплой на Railway:

```bash
# 1. Установите Railway CLI
npm i -g @railway/cli

# 2. Войдите
railway login

# 3. Инициализируйте проект
railway init

# 4. Деплой
railway up
```




