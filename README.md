# 🤖 Система автоматического самосовершенствования Telegram-бота

Автоматическая система, которая тестирует бота, находит ошибки, анализирует их через AI и автоматически улучшает код.

## 🏗️ Архитектура

```
┌─────────────────┐
│  Main Bot       │ ← Основной бот (который улучшаем)
└────────┬────────┘
         │
         │ Общение через эмулятор/Telegram
         │
┌────────▼─────────────────────────────┐
│  Test Client                         │
│  - Отправляет тестовые сообщения      │
│  - Проверяет ответы                   │
└────────┬─────────────────────────────┘
         │
         │ Ошибки + Логи
         │
┌────────▼─────────────────────────────┐
│  Error Monitor                       │
│  - Логирует все диалоги               │
│  - Отслеживает ошибки                 │
└────────┬─────────────────────────────┘
         │
         │ Анализ
         │
┌────────▼─────────────────────────────┐
│  Improvement Agent                   │
│  - Анализирует ошибки через AI        │
│  - Генерирует исправления             │
│  - Обновляет код бота                 │
└──────────────────────────────────────┘
```

## 📁 Структура проекта

```
telegram-bot-self-improvement/
├── main_bot/              # Основной бот (который улучшаем)
│   └── bot.py            # Логика бота
├── test_client/          # Тестовый клиент
│   └── tester.py         # Отправка тестовых сообщений
├── emulator/             # Эмулятор Telegram API
│   └── telegram_emulator.py
├── monitor/              # Мониторинг ошибок
│   └── error_tracker.py
├── improvement_agent/    # Агент самосовершенствования
│   ├── analyzer.py       # Анализ ошибок через AI
│   └── updater.py        # Обновление кода
├── config/
│   └── settings.py       # Настройки
├── logs/                 # Логи (создается автоматически)
├── backup/               # Бэкапы кода (создается автоматически)
├── main.py               # Главный файл запуска
└── requirements.txt      # Зависимости
```

## 🚀 Установка

1. **Клонировать проект** (или создать директорию)

2. **Установить зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настроить переменные окружения:**
```bash
cp .env.example .env
# Отредактировать .env и добавить токены
```

4. **Создать ботов в Telegram:**
   - Через @BotFather создать два бота:
     - Основной бот
     - Тестовый бот
   - Получить токены и добавить в `.env`

## ⚙️ Конфигурация

Файл `.env`:

```env
# Telegram Bot Tokens (опционально, если используете реальный Telegram)
MAIN_BOT_TOKEN=your_main_bot_token_here
TEST_BOT_TOKEN=your_test_bot_token_here

# AI Agent Settings (для автоматических исправлений)
AI_API_KEY=your_openai_api_key_here
AI_MODEL=gpt-4

# Режим работы
USE_REAL_TELEGRAM=false  # Использовать реальный Telegram или эмулятор
EMULATOR_MODE=true       # Режим эмуляции (рекомендуется для начала)

# Настройки тестирования
TEST_GROUP_ID=your_test_group_id_here
```

## 🎯 Использование

### Одноразовое тестирование:
```bash
python main.py --mode single
```

### Непрерывное улучшение (автоматический цикл):
```bash
python main.py --mode continuous --cycles 10 --delay 5
```

Параметры:
- `--mode`: `single` (одно тестирование) или `continuous` (непрерывное улучшение)
- `--cycles`: количество циклов улучшения (по умолчанию 5)
- `--delay`: задержка между циклами в секундах (по умолчанию 5)

## 🔄 Как это работает

1. **Запуск основного бота** - бот инициализируется в эмуляторе или реальном Telegram
2. **Тестирование** - тестовый клиент отправляет набор сообщений и проверяет ответы
3. **Логирование** - все диалоги и ошибки сохраняются в `logs/`
4. **Анализ** - AI-агент анализирует ошибки и генерирует предложения по исправлению
5. **Улучшение** - код бота автоматически обновляется, создается бэкап в `backup/`
6. **Повтор** - цикл повторяется до достижения идеального состояния

## 📊 Логи

Все логи сохраняются в директории `logs/`:
- `conversations.jsonl` - все диалоги
- `errors.jsonl` - все найденные ошибки

## 🔒 Бэкапы

Перед каждым обновлением кода создается резервная копия в `backup/` с timestamp.

## 🎨 Настройка сценариев тестирования

Редактируйте `test_client/tester.py` для добавления новых сценариев:

```python
DEFAULT_TEST_SCENARIOS = [
    ["Привет", "Как дела?", "Пока"],
    ["Ваш сценарий здесь"],
]
```

## 🤖 AI-интеграция

Система поддерживает интеграцию с OpenAI API для автоматической генерации исправлений кода.

Для использования:
1. Добавьте `AI_API_KEY` в `.env`
2. Укажите модель (например, `gpt-4`)

Без AI-ключа система будет использовать простые эвристические исправления.

## 📝 Развитие проекта

- [ ] Поддержка реального Telegram API
- [ ] Больше сценариев тестирования
- [ ] Метрики качества ответов
- [ ] A/B тестирование разных версий бота
- [ ] Веб-интерфейс для мониторинга

## ⚠️ Внимание

Система автоматически изменяет код бота! Всегда проверяйте изменения перед деплоем в production.

## 📄 Лицензия

MIT

